{
  "name": "wick",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5173",
    "test": "node scripts/validate-levels.mjs",
    "new-level": "node scripts/new-level.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
